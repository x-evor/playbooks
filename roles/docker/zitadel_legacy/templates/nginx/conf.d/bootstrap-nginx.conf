server {
    listen 80;
    server_name {{ zitadel_domain }};

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 不 redirect！不要 https！
    # certbot 需要纯 http 验证
}
