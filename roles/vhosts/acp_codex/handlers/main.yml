---
- name: Reload caddy
  ansible.builtin.service:
    name: caddy
    state: reloaded

- name: Restart codex app server
  ansible.builtin.service:
    name: "{{ acp_codex_service_name }}"
    state: restarted
