---
- name: Reload caddy
  ansible.builtin.service:
    name: caddy
    state: reloaded

- name: Restart opencode acp
  ansible.builtin.service:
    name: "{{ acp_opencode_service_name }}"
    state: restarted
