---
- name: Restart xworkmate bridge
  ansible.builtin.systemd:
    name: "{{ xworkmate_bridge_service_name }}"
    state: restarted
    daemon_reload: true

- name: Reload caddy
  ansible.builtin.systemd:
    name: caddy
    state: reloaded
